<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>SociApp - Login</title>
  <link rel="stylesheet" href="stylesheets/monochrome.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"
    integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA=="
    crossorigin="anonymous" referrerpolicy="no-referrer" />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
</head>

<body>
  <div class="login-container">
    <div class="login-card">
      <div class="login-header">
        <img src="images/profile.webp" alt="SociApp Logo" class="login-logo" />
        <h1>Welcome Back</h1>
        <p>Sign in to your account</p>
      </div>

      <% if(error && error.length> 0){ %>
        <div class="error-message">
          <i class="fas fa-exclamation-circle"></i>
          <%= error %>
        </div>
        <% } %>

          <form action="/login" method="post" class="login-form">
            <div class="form-group">
              <input type="text" name="username" placeholder="Username" class="form-input" required />
            </div>
            <div class="form-group">
              <input type="password" name="password" placeholder="Password" class="form-input" required />
            </div>
            <button type="submit" class="btn btn-primary w-full">Sign In</button>
          </form>

          <div class="login-footer">
            <p>Don't have an account? <a href="/" class="link">Sign up</a></p>
          </div>
    </div>
  </div>

  <style>
    .login-container {
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      background: linear-gradient(135deg, var(--off-white) 0%, var(--lightest-gray) 100%);
      padding: 2rem;
    }

    .login-card {
      background: var(--pure-white);
      border-radius: 16px;
      box-shadow: 0 8px 32px var(--shadow);
      padding: 3rem;
      width: 100%;
      max-width: 400px;
      border: 1px solid var(--border-gray);
    }

    .login-header {
      text-align: center;
      margin-bottom: 2rem;
    }

    .login-logo {
      width: 80px;
      height: 80px;
      border-radius: 50%;
      margin-bottom: 1.5rem;
      border: 3px solid var(--border-gray);
    }

    .login-header h1 {
      font-size: 2rem;
      font-weight: 700;
      color: var(--primary-black);
      margin-bottom: 0.5rem;
    }

    .login-header p {
      color: var(--light-gray);
      font-size: 1rem;
    }

    .error-message {
      background: #fef2f2;
      border: 1px solid #fecaca;
      color: #dc2626;
      padding: 0.75rem 1rem;
      border-radius: 8px;
      margin-bottom: 1.5rem;
      display: flex;
      align-items: center;
      gap: 0.5rem;
      font-size: 0.9rem;
    }

    .login-form {
      margin-bottom: 2rem;
    }

    .login-footer {
      text-align: center;
      padding-top: 1.5rem;
      border-top: 1px solid var(--border-gray);
    }

    .login-footer p {
      color: var(--light-gray);
      margin: 0;
    }

    .link {
      color: var(--primary-black);
      text-decoration: none;
      font-weight: 600;
      transition: color 0.3s ease;
    }

    .link:hover {
      color: var(--medium-gray);
    }

    .w-full {
      width: 100%;
    }
  </style>
</body>

</html>